# Arkan Theme — Entity Relationship Diagram
# أركان ثيم — مخطط العلاقات

> 2 DocTypes

```mermaid
erDiagram
    ATSettings ||--|| ATSettings : singleton
```

> **Note:** This is a placeholder ERD. Update with actual DocType relationships from the JSON definitions.
> Run: `ls arkan_theme/arkan_theme/*/doctype/` to discover all DocTypes and their Link fields.
